const config= require('./src/config')

module.exports = {
    siteMetadata: {
      title: `Matt Cacho`,
      siteUrl: `https://www.mattcacho.com`
    },
    plugins: [
      'gatsby-plugin-postcss',
    ]
}